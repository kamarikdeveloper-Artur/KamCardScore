# Mariage result assets

Optional SVG result artwork can be added here later. The Mariage presentation map currently references semantic asset candidates and falls back to text or emoji when a file is absent.
